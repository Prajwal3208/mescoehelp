def sort(l,key):
    size=len(l)
    for i in range(size-1):
        min=i
        for j in range(i+1,size):
            if l[j] < l[min]:
                min=j
        l[i],l[min]=l[min],l[i]
        dis(l)